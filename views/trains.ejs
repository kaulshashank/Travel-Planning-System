<% include ./partials/header %>

<div class="ui main text container">
  <div class="ui huge header"> Select a train </div>
  <div class="ui top attached segment">
      <form action="/plan" method="post" class = "ui form">
        <table class="ui celled table">
          <thead>
            <tr>
              <th></th>
              <th>Train Name/Number</th>
              <th>Origin Station (Code)</th>
              <th>Departs Origin At</th>
              <th>Travel Time</th>
              <th>Destination Station (Code)</th>
              <th>Arrives Destination At</th>
            </tr>
          </thead>
          <tbody>
            <% for(var i=0; i < dat.trains.length; i++) { %>
              <tr>
                <td class="collapsing">
                  <div class="ui fitted slider checkbox">
                    <input name="selected_train" value="<%= dat.trains[i].to_station.code %>" type="checkbox"> <label></label>
                  </div>
                </td>
                <td>
                  <%= dat.trains[i].name +' / ' + dat.trains[i].number %>
                </td>
                <td>
                  <%= dat.trains[i].from_station.name %>
                </td>
                <td>
                  <%= dat.trains[i].src_departure_time %>
                </td>
                <td>
                  <%= dat.trains[i].travel_time %>
                </td>
                <td>
                  <%= dat.trains[i].to_station.name %>
                </td>
                <td>
                  <%= dat.trains[i].dest_arrival_time %>
                </td>
              </tr>
            <% } %>
          </tbody>
        <table>
        <div class="ui right floated small primary labeled icon button" onClick="javascript:this.parentNode.submit();">
          Select Hotels >
        </div>
      </form>
  </div>
</div>

<% include ./partials/footer %>
